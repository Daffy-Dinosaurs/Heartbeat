
export default function(state = null, action) {
  switch (action.type) {
    // case 'CLEAR_COUNTRY':
    //
    //   console.log('this is state inside of clear country', state);
    //   return state;
  }
  return state;
}
